# Example Usage

- `Use $supabase-crud-generator to create CRUD helpers for reports.`
- `Use $supabase-crud-generator to create createRecord and getRecords for samples.`
- `Use $supabase-crud-generator to add CRUD utilities for client portal requests.`

Example output request:

- `Generate TypeScript Supabase CRUD functions for an inventory table with id, name, quantity, and status.`
