# Example Usage

- `Use $rbac-auth-system to create the profiles table and role helpers.`
- `Use $rbac-auth-system to protect the reports page so only admin and lab_manager can approve reports.`
- `Use $rbac-auth-system to add middleware for client portal pages.`

Example output request:

- `Implement Supabase RBAC with a profiles table, role enum, and middleware checks for admin, scientist, technician, and client routes.`
