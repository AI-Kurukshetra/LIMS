# supabase-table-generator

This skill creates SQL migrations for Supabase tables.

It follows a standard structure so new LIMS modules can be added without inventing table rules each time. Every generated table includes UUID IDs, timestamps, and the database relationships needed for future growth.
